import React from 'react';

// Styles
import useStyles from './styles';

function Form() {
    const classes = useStyles();
    return (
        <div>
            <h1>TEAM</h1>
        </div>
    )
}

export default Form
